/**
 * トップページ。F1 では共通レイアウト（Header / Footer）の確認用プレースホルダ。
 * F2 で Hero / BrandConcept / Lookbook / カテゴリ別グリッド / AboutSection を実装する。
 */
export default function HomePage() {
  return (
    <section className="mx-auto max-w-6xl px-6 py-32 text-center">
      <h1 className="text-2xl font-medium tracking-[0.3em] uppercase">EC-PORTFOLIO</h1>
      <p className="mt-6 text-xs tracking-[0.2em] text-graphite uppercase">
        Coming soon
      </p>
    </section>
  );
}
